{
  "name": "web-reader",
  "version": "0.0.1",
  "description": "Write HTML snippets to disk to read later",
  "scripts": {
    "dev": "webextension-toolbox dev",
    "build": "webextension-toolbox build"
  },
  "devDependencies": {
    "serverless-offline": "^8.5.0",
    "webextension-toolbox": "latest"
  }
}
